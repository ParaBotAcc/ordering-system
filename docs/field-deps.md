# 项目场外依赖 —— 由 master 提供

## 1. Docker 数据库密码
```
# 在 docker-compose.yml 中
MYSQL_ROOT_PASSWORD: ?   # 你设一个
MYSQL_PASSWORD: ?         # ordering 用户的密码
```
建议你直接改成你想要的，或者我设好跑之前告知你。

## 2. 飞书应用（供后端调用 Bitable API）
Spring Boot 后端需要通过飞书 API 读写多维表格。选项：
- **A) 沿用现有应用** `cli_aa9c0e8b7dbe1cbc` → 需要在飞书开放平台给它加「多维表格」权限 → 重新发布
- **B) 新建一个飞书应用** → 专用于点餐系统，职责分离
- **C) 初期先跳过** → 点餐核心走 MySQL + Redis，飞书集成后续再加（推荐，先看 Demo）

我建议选 **C）**，先让业务逻辑跑起来，飞书集成后面补。你觉得呢？

## 3. 微信小程序（已就绪）
```
AppID: wx94a58dc2d122b78e   ✅
```
剩下的我直接用微信开发者工具的本地开发模式，不需要额外配置。

---

**总结：** 你只需要告诉我——MySQL 密码设多少 + 飞书要不要现在接。其它我自己搞定。
